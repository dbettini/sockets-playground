{
  "name": "sockets-playground",
  "version": "1.0.0",
  "engines": {
    "node": ">=7"
  },
  "description": "Playground for a websocket based chat app with push notifications",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node --harmony index.js",
    "build": "buble -i app -o public/dist",
    "watch": "watch 'npm run build' app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dbettini",
  "dependencies": {
    "express": "^4.14.0",
    "lodash": "^4.17.2",
    "moniker": "^0.1.2",
    "socket.io": "^1.7.2",
    "web-push": "^3.2.1"
  },
  "devDependencies": {
    "buble": "^0.14.2",
    "watch": "^1.0.1"
  }
}
